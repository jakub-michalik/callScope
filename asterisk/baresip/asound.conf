# Route ALSA "default" through the ALSA->PulseAudio plugin, so baresip's alsa
# module reaches the host's PipeWire/PulseAudio (shared, mixed) instead of
# grabbing a raw hw: device — which would conflict with PipeWire on the host.
pcm.!default {
    type pulse
}
ctl.!default {
    type pulse
}
